import React from 'react';

const PrimeraApp = ( {saludo} ) => {

    // console.log(saludo);
    return (
        <>
            <h1>Helloo, {saludo}</h1>
            <p>Primer parrafo</p>
        </>
    );
};

export default PrimeraApp;